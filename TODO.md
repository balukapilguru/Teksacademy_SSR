# TODO

## Branch page JSON section integration

1. Fix broken component: `src/components/coursePage/AboutTeks.jsx` (currently malformed JSX, missing imports).
2. Ensure `BranchClient.jsx` continues to pass `data.aboutTeksAcandemy` into `AboutTeks` without changing props contract.
3. Component updated to prop-driven JSON renderer (title/heading/description/button/statistics/image).
4. Next: run lint/build in correct shell to verify no TS/ESLint/syntax errors.


